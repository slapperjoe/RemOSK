<UserControl x:Class="RemOSK.Controls.TrackpadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/> <!-- Touch Area -->
            <RowDefinition Height="40"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Touch Surface - Touch is converted to mouse by RegisterTouchWindow -->
        <Border Grid.Row="0" Background="#33FFFFFF" BorderBrush="#55FFFFFF" BorderThickness="1" CornerRadius="4,4,0,0"
                Stylus.IsPressAndHoldEnabled="False"
                Stylus.IsFlicksEnabled="False"
                Stylus.IsTapFeedbackEnabled="False"
                Stylus.IsTouchFeedbackEnabled="False"
                MouseDown="TouchSurface_MouseDown"
                MouseMove="TouchSurface_MouseMove"
                MouseUp="TouchSurface_MouseUp">
            <TextBlock Text="Trackpad" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#88FFFFFF" IsHitTestVisible="False"/>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Click -->
            <Button Grid.Column="0" Content="L" Margin="0,2,1,0" Background="#444" Foreground="White" Click="LeftButton_Click"/>
            
            <!-- Right Click -->
            <Button Grid.Column="1" Content="R" Margin="1,2,0,0" Background="#444" Foreground="White" Click="RightButton_Click"/>
        </Grid>
    </Grid>
</UserControl>
