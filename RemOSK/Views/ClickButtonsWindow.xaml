<local:DraggableWindow x:Class="RemOSK.Views.ClickButtonsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RemOSK.Views"
        Title="RemOSK Clicks" 
        Height="50" Width="160"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Stylus.IsPressAndHoldEnabled="False"
        Stylus.IsFlicksEnabled="False"
        Stylus.IsTapFeedbackEnabled="False"
        ShowActivated="False">
    <Grid>
        <Border x:Name="MainBorder" Background="#CC000000" CornerRadius="10" BorderBrush="#55FFFFFF" BorderThickness="2"
                IsManipulationEnabled="False">
            <Border.LayoutTransform>
                <ScaleTransform x:Name="WindowScaleTransform" ScaleX="1.0" ScaleY="1.0"/>
            </Border.LayoutTransform>
            <!-- Buttons container - hidden in edit mode for easier dragging -->
            <Grid x:Name="ButtonsContainer" Margin="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left Click Button -->
                <Button x:Name="LmbButton" Grid.Column="0" Content="L" FontSize="14" FontWeight="Bold"
                        Background="#555" Foreground="White" BorderBrush="#777"
                        Click="LmbButton_Click"/>
                
                <!-- Middle Click Button -->
                <Button x:Name="MmbButton" Grid.Column="2" Content="M" FontSize="14" FontWeight="Bold"
                        Background="#555" Foreground="White" BorderBrush="#777"
                        Click="MmbButton_Click"/>
                
                <!-- Right Click Button -->
                <Button x:Name="RmbButton" Grid.Column="4" Content="R" FontSize="14" FontWeight="Bold"
                        Background="#555" Foreground="White" BorderBrush="#777"
                        Click="RmbButton_Click"/>
                
                <!-- Hold Toggle Button (smaller) -->
                <ToggleButton x:Name="HoldButton" Grid.Column="6" Content="H" FontSize="10" FontWeight="Bold"
                        Background="#444" Foreground="#AAA" BorderBrush="#666"
                        Checked="HoldButton_Checked" Unchecked="HoldButton_Unchecked"/>
            </Grid>
        </Border>
    </Grid>
</local:DraggableWindow>
