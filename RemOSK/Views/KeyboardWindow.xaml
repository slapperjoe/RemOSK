<Window x:Class="RemOSK.Views.KeyboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemOSK.Views"
        mc:Ignorable="d"
        Title="RemOSK Keyboard" 
        Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize">
    <Grid>
        <!-- Background: #99111111 (~60% Opacity), Border: #88FFFFFF (Visible White Stroke), Thickness: 2px -->
        <!-- Enabled Manipulation for Pinch-to-Zoom and Drag-to-Move -->
        <Border x:Name="MainBorder" Background="#99111111" BorderBrush="#88FFFFFF" BorderThickness="2" CornerRadius="10" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" IsManipulationEnabled="True" ManipulationDelta="MainBorder_ManipulationDelta" ManipulationStarting="MainBorder_ManipulationStarting" ManipulationStarted="MainBorder_ManipulationStarted" ManipulationInertiaStarting="MainBorder_ManipulationInertiaStarting">
            <Border.LayoutTransform>
                <ScaleTransform x:Name="WindowScaleTransform" ScaleX="1.0" ScaleY="1.0"/>
            </Border.LayoutTransform>
            <Grid>
                <Canvas x:Name="KeysCanvas"/>
                <!-- Exit Edit Mode Button (Visible only in Edit Mode) -->
                <Button x:Name="ExitEditModeButton" Content="X" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0" 
                        Background="Red" Foreground="White" FontWeight="Bold" FontSize="16" Visibility="Hidden" Click="ExitEditModeButton_Click" IsManipulationEnabled="False">
                    <Button.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>
    </Grid>

</Window>
